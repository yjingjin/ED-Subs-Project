# jobs/

Databricks Workflows / job definitions that orchestrate the pipeline (ingestion → bronze →
silver → gold → modeling). Add job specs here (e.g. Databricks Asset Bundle YAML or exported
job JSON) once the pipeline stages are concrete.
