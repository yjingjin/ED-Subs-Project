# Local development dependencies (PyCharm).
# On Databricks, most of these are provided by the runtime; pin there via cluster libraries
# or a Databricks Asset Bundle if needed.

# Core data
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
python-dotenv>=1.0

# Spark (for local development against the same APIs used on Databricks)
pyspark>=3.5

# Modeling (Phase 3)
scikit-learn>=1.3
mlflow>=2.10

# EDA / viz (Phase 2)
matplotlib>=3.7
seaborn>=0.13

# Dev tooling
pytest>=7.4
ruff>=0.4
